@inject NavigationManager NavigationManager

@code {
    [Parameter]
    public string RedirectPath { get; set; } = "/login";

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"{RedirectPath}?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }
}
